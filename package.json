{
  "name": "rungame",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute rungame --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute rungame --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.121.0"
  }
}
